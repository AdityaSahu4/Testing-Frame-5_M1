# First Create Environment using python version 3.12.8
i.e. the `.venv` file
# Install the requirements using the command
`pip install -r requirements.txt`
# Add your NeonDB URL 
In the `.env` file
# Run the main file using command 
`py main.py`
